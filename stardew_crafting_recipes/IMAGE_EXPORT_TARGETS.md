# Remaining Image Export Targets

The only remaining missing source texture is:

- `Textures/DN.SnS/BlessedTools`

Export them into `src/craftables_png/` and keep the auto-generated `patch export` output filenames.
