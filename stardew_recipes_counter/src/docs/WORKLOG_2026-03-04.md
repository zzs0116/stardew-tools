# 2026-03-04 工作记录

## 参考提交

- `7ec8f5e` `Backup stardew_tools snapshot`
- `2661f7b` `Backup stardew_tools after purchase updates`
- `68bb3bb` `Backup stardew_tools user updates`

当前最新本地提交：

- `68bb3bb`

## 今天完成了什么

### 1. 前端：购买类来源的 tooltip 支持货币名称

文件：

- `/Users/edisonzhang/Documents/Projects/stardew_tools/stardew_recipes_counter/src/enhanced_index.html`

改动：

- `purchase` 类型的 tooltip 价格显示逻辑已支持 `details.currency_name`
- 有 `currency_name` 时显示为 `价格：<price> <currency_name>`
- 没有 `currency_name` 时继续显示为 `价格：<price>g`

这意味着像“五彩冰棒”这种用星星币购买的食谱，可以直接在 `final_recipes.json` 里写：

```json
"details": {
  "price": 3000,
  "currency_name": "星星币"
}
```

前端会显示为 `价格：3000 星星币`。

### 2. 食谱来源数据：一批 `purchase` 条目已手工整理

文件：

- `/Users/edisonzhang/Documents/Projects/stardew_tools/stardew_recipes_counter/src/final_recipes.json`

当前状态：

- 一批原本被归类为 `other` 的条目已经整理为：
  - `type: "purchase"`
  - `description: "购买"`
- 其中一部分 `details.shop` 已经改成中文商店名
- 其中一部分 `details.condition` 已经从原始条件串改写成中文说明
- “五彩冰棒”已改成用星星币显示所需货币：
  - `currency: "star_tokens"`
  - `currency_name: "星星币"`

注意：

- `final_recipes.json` 里目前既有“纯自动写入”的购买条目，也有你后续手工校正过的购买条目
- 后续继续改时，不要再用脚本批量覆盖这些 `details`

### 3. 商店配方索引文件已保留，并加了你的人工标记

文件：

- `/Users/edisonzhang/Documents/Projects/stardew_tools/stardew_recipes_counter/src/Data_Shops_IsRecipe.txt`

当前状态：

- 文件仍然是 `Data_Shops.json` 里 `IsRecipe = true` 的总清单
- 你已经在中间加入了两段分隔标记：
  - `----------↓------------//3月3日`
  - `--------↑------------//3月3日`

这部分明显是在标记处理范围，后续不要覆盖这个文件的结构，除非你明确要重生成。

### 4. 购买候选名单已从 `.txt` 改成 `.md`

文件：

- `/Users/edisonzhang/Documents/Projects/stardew_tools/stardew_recipes_counter/src/other_source_shop_recipe_names.md`

当前状态：

- 原来的 `.txt` 已删除
- 改成了 `.md`
- 你已经用 Markdown 删除线 `~~ ~~` 标记了一部分已处理项

这个文件现在适合作为“待处理清单”继续推进。

## 明天从哪里开始

建议按下面顺序继续：

1. 先打开 `/Users/edisonzhang/Documents/Projects/stardew_tools/stardew_recipes_counter/src/other_source_shop_recipe_names.md`

目标：

- 继续处理还没有删除线的条目
- 每处理完一个，就在这个列表里标记掉

2. 逐条对照 `/Users/edisonzhang/Documents/Projects/stardew_tools/stardew_recipes_counter/src/final_recipes.json`

重点检查每个 `purchase` 条目的：

- `description` 是否保持为 `购买`
- `details.shop` 是否已改成你想要的中文店名
- `details.price` 是否正确
- 如果不是金币购买：
  - 是否补了 `currency_name`
  - 或者是否应该改成兑换信息（`trade_item_id` / `trade_item_amount`）
- `details.condition` 是否已经被你改成最终想展示的中文说明

3. 需要查商店原始信息时，只查这两个文件

- `/Users/edisonzhang/Documents/Projects/stardew_tools/stardew_recipes_counter/src/Data_Shops_IsRecipe.txt`
- `/Users/edisonzhang/Documents/Projects/stardew_tools/stardew_recipes_counter/src/Data_Shops.json`

用途：

- `Data_Shops_IsRecipe.txt` 用来快速定位
- `Data_Shops.json` 用来确认原始字段（`Price`、`TradeItemId`、`Condition`、`PerItemCondition`）

## 明天不要先做的事

- 不要先重跑任何会批量改写 `final_recipes.json` 的脚本
- 不要重生成 `Data_Shops_IsRecipe.txt`，除非你已经不需要保留里面的人工分隔标记
- 不要批量用商店数据覆盖 `details.condition`，现在里面已经有人工整理过的中文说明

## 如果需要回退

可以先看这两个提交：

- `2661f7b`：购买更新后的备份
- `68bb3bb`：你后续手工修改后的备份

如果明天要对比今天后半段你手工改了什么，优先比较：

- `2661f7b..68bb3bb`

## 一句话交接

明天的起点不是“重建数据”，而是“继续手工清理剩余的购买来源条目”，以 `other_source_shop_recipe_names.md` 为待办清单，以 `final_recipes.json` 为最终结果文件。
